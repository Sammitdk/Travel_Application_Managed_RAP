projection;
strict ( 2 );

define behavior for ZC_TRAVEL_77_M //alias <alias_name>
use etag
{
  use create;
  use update;
  use delete;

  use action copytravel;
  use association _booking { create; }
}

define behavior for ZC_BOOKING_77_M //alias <alias_name>
{
  use update;
  use delete;

  use association _travel;
  use association _booking_supp { create; }
}

define behavior for ZC_BOOKING_SUPP_77_M //alias <alias_name>
{
  use update;
  use delete;

  use association _travel;
  use association _booking;
}